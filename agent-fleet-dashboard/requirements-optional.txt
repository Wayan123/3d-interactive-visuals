# Optional Python dependencies for CAD export pipeline.
# NOT auto-installed. Only required if you want to regenerate
# STEP / STL / GLB from cad_source/*.py via the text-to-cad skill.
#
# Install manually with Python 3.11:
#   python3.11 -m venv .venv
#   ./.venv/bin/pip install -r requirements-optional.txt
#
# The browser dashboard at http://127.0.0.1:8877/ does NOT need these.

build123d>=0.7.0
cadquery-ocp>=7.7.0
trimesh>=4.0.0
numpy>=1.24
pillow>=10.0

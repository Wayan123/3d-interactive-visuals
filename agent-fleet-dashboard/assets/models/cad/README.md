# DEPRECATED — moved to `cad_source/`

Folder ini dulunya sebagai drop zone untuk CAD models manual. Sekarang semua CAD source dan render 3D sudah internal di project:

- **CAD Python sources**: `/cad_source/` (build123d, optional)
- **Runtime 3D**: `/assets/bio/geometry.js` (procedural Three.js)

Lihat:

- `/docs/BIOCELL_ARCHITECTURE.md`
- `/cad_source/README.md`

Tidak perlu meng-drop `.glb`/`.step` di sini.
